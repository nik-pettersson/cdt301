int fac (int i)
{
   if (i <= 1)
      return 1;
   else
      return (i * fac (i - 1));
}

void trac42 (void)
{
   int x,y,z;

   read x;
   while (!(x == 0)) {
      write (fac (x) / 2);
      x = x - 2;
   }
}
